package com.example.endangeredanimals.ui

import androidx.compose.ui.graphics.Color

// === BẢNG MÀU CHỦ ĐẠO (Primary: Dải màu Xanh Thiên Nhiên) ===
// Mốc Green500 chính là màu AppPrimaryColor (0xFF37ab3c) cũ của bạn.
val Green50  = Color(0xFFE7F5E8) // Xanh cực nhạt (dùng làm nền cho Item được chọn)
val Green100 = Color(0xFFC3E6C5) // Nền cho các Card thông báo
val Green200 = Color(0xFF9BD59F)
val Green300 = Color(0xFF73C379)
val Green400 = Color(0xFF55B55C) // Xanh nhạt hơn một chút (dùng khi hover)
val Green500 = Color(0xFF37AB3C) // MÀU CHÍNH TÂM
val Green600 = Color(0xFF319C35) // Xanh đậm hơn (dùng khi bấm vào nút)
val Green700 = Color(0xFF2A8A2D)
val Green800 = Color(0xFF237926)
val Green900 = Color(0xFF17591A) // Xanh siêu đậm (dùng cho chữ trên nền xanh nhạt)

// === BẢNG MÀU ĐIỂM NHẤN & CẢNH BÁO ===
val AccentBrown = Color(0xFF8F8351)    // AppButtonChangePW cũ (Màu đất mộc mạc)
val AccentOrange = Color(0xFFAB310F)   // Secondary cũ (Màu gạch nung)
val ErrorRed = Color(0xFFB00020)       // AppWarningColor cũ (Màu đỏ chuẩn Material)

// === BẢNG MÀU TRUNG TÍNH (Background, Surface, Text) ===
val Neutral50  = Color(0xFFFAFAFA)     // Trắng ngà (Nền toàn App)
val Neutral100 = Color(0xFFF0EDED)     // AppBackgroundCard cũ
val Neutral200 = Color(0xFFEBEBEB)     // AppBackgroundColor cũ
val Neutral600 = Color(0xFF536670)     // AppGrayBlue cũ (Xám ánh xanh - Text phụ)
val Neutral800 = Color(0xFF2D373C)     // Chữ đen chính (Dùng màu này đỡ đau mắt hơn màu đen tuyền)

// === MÀU RIÊNG LẺ CỦA GIAO DIỆN ===
val BottomNavBackground = Color(0xC9CCD5CC) // Giữ nguyên nền của BottomBar